# Company Brain — environment variables
#
# By default this agent uses OAuth-connected accounts (Slack, Notion, Google Drive, Web Search)
# and needs NO keys here.
# Only fill these in if a knowledge source requires an API key that isn't handled by the OAuth
# connection.

# Example (uncomment and set if needed):
# KNOWLEDGE_SOURCE_API_KEY=your-source-api-key
