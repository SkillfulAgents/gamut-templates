# Prospect Finder — environment variables
#
# By default this agent uses OAuth-connected accounts (CRM, Slack) and platform-native web search,
# and needs NO keys here. Only fill these in if your CRM requires an API key that isn't handled by the
# OAuth connection.

# Example (uncomment and set if needed):
# CRM_API_KEY=your-crm-api-key
