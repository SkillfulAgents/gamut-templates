# Content Engine — environment variables
#
# By default this agent uses OAuth-connected accounts (Notion or Google Drive, Slack, your
# transcript source, web search) and needs NO keys here.
# Only fill these in if a connected service requires an API key that isn't handled by OAuth.

# Example (uncomment and set if needed):
# WEB_SEARCH_API_KEY=your-web-search-api-key
