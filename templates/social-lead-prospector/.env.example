# Social Lead Prospector — environment variables
# Copy this file to .env and fill in any values needed.
# The agent will guide you through setup during onboarding.

# Optional: API key if connecting a CRM or contact list for deduplication
# CONTACTS_API_KEY=

# Optional: CSV path for existing-customer list (local file)
# EXISTING_CUSTOMERS_CSV=reports/existing-customers.csv
